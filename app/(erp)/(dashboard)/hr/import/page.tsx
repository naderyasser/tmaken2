'use client'

// F10 — Data import route. Under /hr → HrShell + HR gate already applied by the layout.
import { ImportTool } from '@/components/import/import-tool'

export default function ImportPage() {
  return <ImportTool />
}
