'use client'

// F9 — Analytics Hub route. Under /hr → HrShell + HR gate already applied by the layout.
import { AnalyticsHub } from '@/components/analytics/analytics-hub'

export default function AnalyticsPage() {
  return <AnalyticsHub />
}
