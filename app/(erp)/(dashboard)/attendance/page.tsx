/**
 * «اضافة و تعديل حركات» — Apex layout (report filter grid + add), inside the
 * unified HR shell (HrGuard owns the HR gate).
 */

'use client'

import { MovementsPage } from '@/components/hr/movements-page'

export default function AttendancePage() {
  return <MovementsPage />
}
