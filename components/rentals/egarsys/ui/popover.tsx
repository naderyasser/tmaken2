// Scoped re-export of the platform's shadcn `popover` primitive, so the ported
// egarsys contracts-summary screen imports every UI primitive from one folder.
export * from '@/components/ui/popover'
